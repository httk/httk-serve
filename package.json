{
  "name": "httk-serve-browser-tests",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@playwright/test": "1.62.1"
  },
  "scripts": {
    "test-js": "node --test tests/serve-optimade-table-protocol.test.mjs tests/serve-optimade-table-controller.test.mjs",
    "test-browser": "playwright test tests/serve-optimade-table.browser.spec.mjs"
  }
}
