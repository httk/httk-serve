# Examples

- `modern/` and `legacy/` contain web examples.
- `optimade/store_server/` is the preferred live-store OPTIMADE server;
  the other `optimade/` examples cover providers and lower-level adapters.
- `dsp_store_catalogue/` composes public files with a store-native DSP catalogue.
